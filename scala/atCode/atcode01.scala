/**
 *
 */

//object Main extends App {
  /*
  def code(a: String, b: String, c: String): String = {
    if(a.length >= 5 && b.length >= 5 && c.length >= 5 && (a.length + b.length + c.length) == 17){
      "YES"
    } else {
      "NO"
    }
  }
  println(code("ddddd","ddddd","ddddddd"))
  
 var a = readInt
 var b = readInt
 var c = readInt
 println(if(a >= 5 && b >= 5 && c >= 5 && (a + b + c) == 17){
   "YES"
 } else {
   "NO"
 })
 */
/*
  val sc = new java.util.Scanner(System.in)
  println(if (List.fill(3)(sc.nextInt()).sorted == List(5, 5, 7)) "YES" else "NO")
}
*/
